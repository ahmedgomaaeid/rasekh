
    <a class="nav-link icon" data-bs-toggle="dropdown" wire:click="seen" id="dropdown-icon">
        <i class="fe fe-bell"></i>
        @if($seen_sign != 0)
        <span class=" pulse"></span>
        @endif
    </a>

